"""GitHub 宝藏雷达 (Treasure Radar)。

从 GitHub 每日新增/变更的仓库中，用「增速信号 + 领域分类 + LLM 语义研判」
挖掘尚未被大众发现的宝藏项目，按垂直频道生成每日报告。

设计要点：
* 仓库级数据一律走 REST/GraphQL API，绝不抓 HTML 详情页（合规红线）。
* 全局令牌桶 + 请求预算 + 403/429 退避 + ETag 条件请求，绝不打满配额。
"""

__version__ = "1.0.0"
