# GitHub 宝藏雷达 —— 依赖清单
# 核心采集/打分/存储全部使用 Python 标准库，以下均为可选增强依赖。
# 未安装时程序会自动降级：HTML 用内置简易模板、调度用内置循环调度器。

jinja2>=3.1          # HTML 报告模板（缺失时自动降级为内置字符串模板）
apscheduler>=3.10    # 长驻调度模式（缺失时自动降级为内置循环调度器）
tzdata>=2024.1       # Windows 下 zoneinfo 时区数据（报告日期按配置时区；缺失时回退 UTC）

# 以下为可选，按需取消注释
# httpx>=0.27        # 若希望用 httpx 替代内置 urllib 传输层
# duckdb>=1.0        # 对 SQLite 做大规模时序分析
# pytest>=8.0        # 运行 tests/
